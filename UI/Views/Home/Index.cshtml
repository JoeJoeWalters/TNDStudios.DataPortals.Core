@{
    ViewData["Title"] = "Home Page";
}

@section Scripts
    {
    <script src="~/js/pages/test.js"></script>
}

<div class="row">
    <div class="col-md-12">
        <div id="test">
            <button class="btn" v-on:click="load" style="margin-top: 50px">Load Data</button>
            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th v-for="item in definition.itemProperties">
                            {{ item.name }}
                        </th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td v-for="item in definition.itemProperties">
                            {{ item.pattern }}
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>

    </div>
</div>
